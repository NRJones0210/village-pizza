include ../layout

  .container
    h1= title
    for order in orders
      section
        h1= order.user.name
        ul
          for pizza in order.orderPizzas
            li= pizza.name
            ul
              for topping in pizza.toppings
                li= topping.name

include ../footer
  script.
    $("#orders").addClass("active");
